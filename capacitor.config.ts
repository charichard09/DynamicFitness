import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dynamicfitness.app',
  appName: 'Dynamic Fitness',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
